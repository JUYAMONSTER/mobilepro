name: closely
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1

  path_provider: ^2.0.12
  image_picker: ^0.8.7+3
  shared_preferences: ^2.1.1
  image_picker_web: ^2.0.4
  image: ^3.0.1

  firebase_core: ^3.8.1
  firebase_auth: ^5.3.4
  google_sign_in: ^6.2.2
  google_sign_in_web: ^0.12.4+3
  googleapis: ^11.0.0
  cupertino_icons: ^1.0.8
  geocoding: ^2.0.5
  # 추가된 패키지들
  google_maps_flutter: ^2.2.5
  google_maps_flutter_web: ^0.5.10 # 웹
  location: ^4.4.0
  http: ^0.13.6
  geolocator: ^9.0.2
  permission_handler: ^10.0.0
  csv: ^6.0.0
  latlong2: ^0.9.1
  utm: ^0.1.3-nullsafety.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
